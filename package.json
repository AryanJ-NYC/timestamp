{
  "name": "timestamp",
  "version": "1.0.0",
  "description": "A REST API that returns a JSON file containing time information.",
  "main": "timestamp.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.4"
  }
}
